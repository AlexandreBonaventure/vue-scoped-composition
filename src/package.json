{
  "name": "vue-scoped-composition",
  "version": "0.0.1",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "dev": "cd examples && vite",
    "build": "cd examples && vite build"
  },
  "devDependencies": {
    "@vue/compiler-sfc": "^3.0.2",
    "esbuild": "^0.7.0",
    "vite": "^1.0.0-rc.9",
    "vue": "next"
  }
}
